html
    head
        title Log In to EddieHinkle.com
        style
            include ../global.css
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
    body
        h1.page-title Log in to EddieHinkle.com
        if error
            p.error=error
        form(action="", method="post")
            input(type="text", name="me", placeholder="domain.com", autofocus="autofocus", onfocus="this.select()").password-input
            input(type="submit", value="Log In").button